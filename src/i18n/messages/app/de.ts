/**
 * App (@eng) i18n slice (de) -- decomposed into modules/ surfaces; see
 * app/en.ts and ../modules/en.ts for the contract.
 */

import { modulesDe } from "../modules/de";

export const appDe = {
  ...modulesDe,
} as const;
