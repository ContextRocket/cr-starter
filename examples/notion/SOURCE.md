# Notion

- Crawl: `scratchpad/brand-crawls/notion-so/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Get Notion free). Font: NotionInter → Google Inter (nearest). Logo: wordmark-paths lightFit=false
