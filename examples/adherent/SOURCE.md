# Adherent

- Crawl: `scratchpad/brand-crawls/adherent-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Book a Demo). Font: Aeonik → Google Inter (nearest). Logo: wordmark-paths lightFit=true
