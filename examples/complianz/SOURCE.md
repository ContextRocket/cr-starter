# Complianz

- Crawl: `scratchpad/brand-crawls/complianz-io/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (WordPress Plugin). Font: Roboto Slab → Google Inter (nearest). Logo: transparent-mark lightFit=true
