# Osano

- Crawl: `scratchpad/brand-crawls/osano-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: body bg transparent → assumed white canvas; CTA fill → primary (Get a Demo). Font: Jost → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="Logo"
