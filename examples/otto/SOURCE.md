# OTTO

- Crawl: `scratchpad/brand-crawls/otto-de/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: body bg transparent → assumed white canvas; no CTA fill → body ink as primary. Font: OTTOSans → Google Inter (nearest). Logo: default-ok lightFit=true
