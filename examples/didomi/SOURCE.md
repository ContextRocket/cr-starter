# Didomi

- Crawl: `scratchpad/brand-crawls/didomi-io/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Request a demo). Font: Montserrat Custom → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="Didomi"
