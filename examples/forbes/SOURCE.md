# Forbes

- Crawl: `scratchpad/brand-crawls/forbes-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Sign In or Create a Free Account). Font: Graphik → Google Inter (nearest). Logo: wordmark-paths lightFit=false
