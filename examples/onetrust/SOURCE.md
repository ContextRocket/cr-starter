# OneTrust

- Crawl: `scratchpad/brand-crawls/onetrust-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; no CTA fill → body ink as primary; no distinct accent → reuse primary. Font: Antique Legacy → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="OneTrust logo lockup, black"
