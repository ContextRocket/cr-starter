# contextrocket

- Crawl: `scratchpad/brand-crawls/contextrocket-ai/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Sign Up). Font: Inter → Google Inter. Logo: wordmark-paths lightFit=true alt="ContextRocket Logo"
