# posthog

- Crawl: `scratchpad/brand-crawls/posthog-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Get started). Font: RoundHog → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="PostHog"
