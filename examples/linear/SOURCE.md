# linear

- Crawl: `scratchpad/brand-crawls/linear-app/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: dark body → primary/foreground; white canvas assumed; CTA fill → accent (distinct from primary). Font: Inter Variable → Google Inter (nearest). Logo: wordmark-paths lightFit=true
