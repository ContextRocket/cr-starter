# Consent Management Platform (CMP) Usercentrics

- Crawl: `scratchpad/brand-crawls/usercentrics-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Start Free). Font: Roboto Condensed → Google Inter (nearest). Logo: wordmark-paths lightFit=true
