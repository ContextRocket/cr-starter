# Termly

- Crawl: `scratchpad/brand-crawls/termly-io/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: body bg transparent → assumed white canvas; CTA fill → primary (Try for free!). Font: Helvetica → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="Termly logo"
