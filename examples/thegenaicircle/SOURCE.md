# The GenAI Circle

- Crawl: `scratchpad/brand-crawls/thegenaicircle-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Join here). Font: Courier Prime → Google Inter (nearest). Logo: transparent-mark lightFit=true
