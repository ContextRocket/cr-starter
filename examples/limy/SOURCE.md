# Limy

- Crawl: `scratchpad/brand-crawls/limy-ai/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: dark body → primary/foreground; white canvas assumed; CTA fill → accent (distinct from primary). Font: sans-serif → Google Inter (nearest). Logo: light-ink-on-transparent lightFit=false
