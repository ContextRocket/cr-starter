# Mucho

- Crawl: `scratchpad/brand-crawls/wearemucho-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Mucho has transformed more than 250 brands 250+ Discover more →). Font: ABCRepro-Screen → Google Inter (nearest). Logo: wordmark-paths lightFit=false
