# stripe

- Crawl: `scratchpad/brand-crawls/stripe-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: body bg transparent → assumed white canvas; CTA fill → primary (Get started). Font: sohne-var → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="Stripe logo"
