# tryformation

- Crawl: `scratchpad/brand-crawls/tryformation-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: dark body → primary/foreground; white canvas assumed; chromatic h1 → accent (preferred over muted CTA). Font: Fira Mono → Google Inter (nearest). Logo: wordmark-paths lightFit=true
