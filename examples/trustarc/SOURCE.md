# TrustArc

- Crawl: `scratchpad/brand-crawls/trustarc-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Contact us). Font: museo → Google Inter (nearest). Logo: text-only lightFit=true
