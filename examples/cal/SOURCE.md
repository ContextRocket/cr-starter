# Cal.com

- Crawl: `scratchpad/brand-crawls/cal-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; no CTA fill → body ink as primary; no distinct accent → reuse primary. Font: Cal Sans → Google Inter (nearest). Logo: dark-mark-on-clear lightFit=true
