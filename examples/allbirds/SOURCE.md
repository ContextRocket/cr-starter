# Allbirds

- Crawl: `scratchpad/brand-crawls/allbirds-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Shop Womens). Font: Geograph → Google Inter (nearest). Logo: wordmark-paths lightFit=true
