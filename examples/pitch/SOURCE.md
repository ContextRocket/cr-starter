# Pitch

- Crawl: `scratchpad/brand-crawls/pitch-com/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Sign up). Font: Mark Pro Bold → Google Inter (nearest). Logo: transparent-mark lightFit=true alt="Pitch brand library interface"
