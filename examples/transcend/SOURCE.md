# Transcend

- Crawl: `scratchpad/brand-crawls/transcend-io/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: body bg transparent → assumed white canvas; CTA fill → primary (Get a demo). Font: GTPlanar → Google Inter (nearest). Logo: wordmark-paths lightFit=true
