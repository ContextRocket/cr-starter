# Grandpal

- Crawl: `scratchpad/brand-crawls/grandpal-co/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: light body background kept; CTA fill → primary (Sign up for Free). Font: Lora → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="Grandpal"
