# Axeptio

- Crawl: `scratchpad/brand-crawls/axeptio-eu/`
- Method: observe → rank → assign (see `examples/CAPTURE.md`)
- Colors: body bg transparent → assumed white canvas; CTA fill → primary (Get a demo). Font: Figtree → Google Inter (nearest). Logo: wordmark-paths lightFit=true alt="White Axeptio logo"
