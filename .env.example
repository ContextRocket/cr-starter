# ContextRocket Starter -- root environment reference
#
# The public starter is Next.js/static-first. Copy frontend/config/.env.example
# to frontend/.env.local for site and ContextRocket settings.

FRONTEND_PORT=3100

# Optional override used by the local Next server.
STATIC_EXPORT=false
