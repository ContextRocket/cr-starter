/**
 * App (@eng) i18n slice (es) -- decomposed into modules/ surfaces; see
 * app/en.ts and ../modules/en.ts for the contract.
 */

import { modulesEs } from "../modules/es";

export const appEs = {
  ...modulesEs,
} as const;
